namespace BlocKRender {
    export class RenderOptions {
        //
    }

    export class Render {
        private renderOptions: RenderOptions;

        constructor(window: HTMLCanvasElement, options: RenderOptions) {
            this.renderOptions = options;
        }

        render(): void {
            //
        }
    }
}